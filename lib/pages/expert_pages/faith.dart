import 'package:flutter/material.dart';

class FaithExpert extends StatelessWidget {
  const FaithExpert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Faith Experts'),
      ),
    );
  }
}
