import 'package:flutter/material.dart';

class EconomyExperts extends StatelessWidget {
  const EconomyExperts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Economists'),
      ),
    );
  }
}
